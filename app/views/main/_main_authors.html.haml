%section.section
  / section__header
  .section__header
    .section__title.title
      %span.title__text авторские колонки
  / section__main
  .section__main
    / columns
    .columns
      .columns__list
        .row
          - @authors.each do |author|
            = render partial: "other/author", locals: {author: author}