%section.section
  / section__header
  .section__header
    .section__title.title
      %span.title__text полезные статьи
  / section__main
  .section__main
    / articles
    .articles
      .articles__list
        - @articles.each do |article| 
          = render partial: "other/article", locals: {article: article}