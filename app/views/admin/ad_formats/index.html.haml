= render :partial => 'admin/shared/ad_agency/well_menu'
= render :partial => 'search_form'

= link_to 'Новый формат рекламной поверхности', new_admin_ad_format_path
= render :partial => 'table', :locals => { :ad_formats => @ad_formats }

= will_paginate_bootstrap @ad_formats